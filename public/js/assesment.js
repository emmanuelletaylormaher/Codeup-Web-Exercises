//1. Write a function named isNegative that accepts a number and returns true or false based on whether the input is negative.

// function isNegative(i) {
// 	var i;	
// 		if (i < 0) {
// 			return true;
// 		} else {
// 			return false;
// 		}
// }

//2. Write a function named average that accepts an array of numbers and returns the average of those numbers.

function average(array) {
	return array;
}

//3. Write a function named countOdds that accepts an array of numbers and returns the number of odd numbers in the array.

function countOdds(array){
	
}

//4. Write a function named convertNameToObject that accepts a string that contains a first name and last name separated by a space, 
//and returns an object with properties firstName and lastName.

function convertNameToObject(input) {
	var input = {};

}

//5. Write a function named fiveTo that accepts a number and returns an array of the integers from 5 up to (but not including) the passed number.

function fiveTo(i){
	var i;
	if (i < 5) {
		console.log("Please enter a number greater than 5.");
	} else {
		for (var j = 5; j < i; j++) {
			console.log(j);
		}	
	}
}

//6. Write a function named upperCaseLastNames that accepts an array of objects where each object has properties firstName 
//and lastName and returns the same array of objects with each last name capitalized.

function upperCaseLastNames(array){

}